import React from 'react';
import Avatar from '@material-ui/core/Avatar';
import getProductStyles from '../API/GetProductStyles';

const RelatedStyles = ({styles}) => {
  
{/* Need to use product_id > style_id  */}
  // const styleRender = styles.map(style => {
  //   <Avatar alt={} src="" /> 
  // });

  return (
    <div>
      Related Styles will go here.
    </div>
  );
}

export default RelatedStyles;